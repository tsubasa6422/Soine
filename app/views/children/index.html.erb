<div class="container mt-4" style="max-width: 800px;">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h3 class="mb-0">子ども情報</h3>
    <%= link_to "＋ 新規登録", new_child_path, class: "btn btn-primary" %>
  </div>

  <% if @children.any? %>
    <div class="list-group">
      <% @children.each do |child| %>
        <div class="list-group-item d-flex justify-content-between align-items-center">
          <div class="d-flex align-items-center">
            <div class="child-avatar me-3">
              <%= child.name.first.upcase %>
            </div>
            <div>
              <div class="fw-bold"><%= child.name %></div>
              <% if child.respond_to?(:age_label) && child.age_label.present? %>
                <div class="small text-muted"><%= child.age_label %></div>
              <% end %>
              <% if child.respond_to?(:birthday) && child.birthday.present? %>
                <div class="small text-muted">誕生日：<%= child.birthday %></div>
              <% end %>
            </div>
          </div>

          <div class="d-flex gap-2">
            <%= link_to "編集", edit_child_path(child), class: "btn btn-sm btn-outline-secondary" %>
            <%= link_to "削除", child_path(child),
                  method: :delete,
                  data: { confirm: "この子ども情報を削除しますか？" },
                  class: "btn btn-sm btn-outline-danger" %>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="alert alert-info">
      まだ子ども情報がありません。「＋ 新規登録」から追加してください。
    </div>
  <% end %>

  <div class="mt-3">
    <%= link_to "マイページへ戻る", mypage_path, class: "btn btn-outline-secondary" %>
  </div>
</div>
